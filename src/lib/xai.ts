// Deprecated: xAI's Grok API requires a funded team account (no free tier for new
// teams as of mid-2026). Switched to Groq (console.groq.com), which has a genuine
// free developer tier. See src/lib/groq.ts — this file is unused and kept only
// because the sandbox filesystem couldn't delete it (EPERM on this mounted folder).
// Safe to delete manually.
export {};
